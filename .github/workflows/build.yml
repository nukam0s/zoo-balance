name: Build VSIX

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm run compile
      - name: Package VSIX
        run: npx @vscode/vsce package --no-verify-publisher
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: zoo-balance-vsix
          path: '*.vsix'
      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: '*.vsix'
